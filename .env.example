# Postgres connection string (get a free one from Neon, Supabase, or Railway)
DATABASE_URL="postgresql://user:password@localhost:5432/boutique?schema=public"

# Any long random string — generate with: openssl rand -base64 32
JWT_SECRET="replace-this-with-a-long-random-secret"
